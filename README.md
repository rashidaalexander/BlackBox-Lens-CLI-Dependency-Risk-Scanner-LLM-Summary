# BlackBox-Lens-CLI-Dependency-Risk-Scanner-LLM-Summary
 A CLI tool that scans dependency manifests, queries OSV, computes a 0–100 risk score, and optionally generates a defensive LLM summary (Ollama default, OpenAI optional). No HTML.
